run docker build -t jenkins-master jenkins/
docker run -it -p 9999:8080 -v jenkins-vol:/var/jenkins_home jenkins-master
run docker build -t jenkins-worker worker/
upload jenkins worker image to docker hub
